#include <iostream>
#include <deque>
#include <queue>
#include <vector>
using namespace std;

int main(int argc, char *argv[]) {
    int T;
    scanf("%d", &T);

    while (T--) {
        int n, t1, t2, t3, t4, t5, Q;
        queue<int> porgq[n];
        vector<string> progs;
        
        scanf("%d %d %d %d %d %d %d", &n, &t1, &t2, &t3, &t4, &t5, &Q); 

        for (int i = 0; i < n; i++) {
        }
    }
    return 0;
}
